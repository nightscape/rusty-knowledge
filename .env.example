# Vite Configuration
# Port for the development server
VITE_PORT=5173

# Development Mode
# When running in browser (not Tauri), the app will use mock IPC
# This is automatically detected - no configuration needed
